"""
LLM Gateway: Provider-agnostic LLM interface.

Provides unified access to multiple LLM providers with:
- Gemini (SDK + CLI fallback)
- OpenAI GPT models
- Claude (future)
- Cost tracking and credential management
"""
