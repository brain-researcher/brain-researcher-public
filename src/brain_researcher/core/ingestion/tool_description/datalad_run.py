description = [
    {
        "name": "datalad_run",
        "signature": "(cmd: str, dataset: str) -> dict[str, str | int]",
        "description": "Run a command within a DataLad dataset.",
    }
]
