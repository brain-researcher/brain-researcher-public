# Rule

When you want to add, update, or relocate repository instructions that would normally go into `CLAUDE.md`, write them into `AGENTS.md` instead. `AGENTS.md` is the canonical instruction file for this repository.
