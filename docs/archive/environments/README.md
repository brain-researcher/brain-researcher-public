# Archived environment snapshots

Files in this directory are historical records. They are not supported install
contracts and are not used by setup scripts or CI.

Current environments live at the repository root:

- `environment.yml`: full contributor/developer bootstrap
  (`brain_researcher-dev`)
- `environment.brain_researcher.yml`: Studio/Jupyter scientific runtime
  (`brain_researcher`)

`neuroimage_env_2025-08-20.yaml` was last updated on 2025-08-20 and archived
from public commit `8081642b110bca43c9f9a8cd3d056aa86b7497a3` on 2026-07-15.
Its commands, paths, package claims, and test counts are retained only for
provenance and have not been revalidated.
