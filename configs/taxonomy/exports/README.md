# Generated exports live here.

# TODO CMD: after curation, run `python configs/taxonomy/build/aggregate.py --export`
#           to refresh YAML/CSV/Markdown outputs. Consider adding this directory
#           to .gitignore if exports should remain local artifacts.
