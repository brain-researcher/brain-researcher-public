This folder is deprecated for runtime mapping sources.

Canonical mapping files now live under `configs/` and are resolved through
`brain_researcher.config.mapping_resolver`.

- `onvoc_crosswalk.yaml` canonical path:
  `configs/legacy/mappings/onvoc_crosswalk.yaml`
- `cao_to_trm.yaml` canonical path:
  `configs/legacy/mappings/cao_to_trm.yaml`

Do not add new source-of-truth mapping files here.
